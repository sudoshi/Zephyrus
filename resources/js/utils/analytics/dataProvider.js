/**
 * Simplified data provider
 * This file is kept for compatibility but no longer contains complex logic
 */

// Simple pass-through function that returns the data directly
export const fetchORUtilizationData = async (filters) => {
  // This function is no longer used - data is hard-coded in the hook
  return { success: true };
};

// Simple pass-through function that returns the data directly
export const fetchBlockUtilizationData = async (filters) => {
  // This function is no longer used - data is hard-coded in the hook
  return { success: true };
};

// Simple pass-through function that returns the data directly
export const fetchPrimetimeUtilizationData = async (filters) => {
  // This function is no longer used - data is hard-coded in the hook
  return { success: true };
};

// Simple pass-through function that returns the data directly
export const fetchRoomRunningData = async (filters) => {
  // This function is no longer used - data is hard-coded in the hook
  return { success: true };
};

// Simple pass-through function that returns the data directly
export const fetchTurnoverTimesData = async (filters) => {
  // This function is no longer used - data is hard-coded in the hook
  return { success: true };
};
