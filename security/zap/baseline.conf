# ZAP passive scanner policy. Rules omitted here retain ZAP's default WARN
# behavior; -I prevents warnings from becoming a failing exit code. FAIL is
# reserved for controls Zephyrus already owns and treats as release blockers.
10020	FAIL	(X-Frame-Options Header Scanner)
10021	WARN	(X-Content-Type-Options may be absent only on PHP test-server static files; Apache policy blocks release)
10038	FAIL	(Content Security Policy Header Not Set)
10055	WARN	(CSP style-src unsafe-inline compatibility debt; no script unsafe-inline is allowed)
10010	WARN	(XSRF-TOKEN must remain script-readable; the session cookie is HttpOnly)
10011	WARN	(Cookie Without Secure Flag - expected only on loopback HTTP DAST)
10035	WARN	(Strict-Transport-Security Header - TLS is terminated by Apache)
10054	FAIL	(Cookie without SameSite Attribute)
