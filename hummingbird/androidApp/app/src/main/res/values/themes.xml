<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all styling; this platform theme just sets a dark window so launch is
         dark-default. AppCompat parent supports androidx.biometric's pre-API-28 dialog. -->
    <style name="Theme.Hummingbird" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">#0F172A</item>
        <item name="android:statusBarColor">#0F172A</item>
        <item name="android:navigationBarColor">#0F172A</item>
    </style>

    <!-- Android 12+ SplashScreen API: branded dark launch that hands off to the app theme. -->
    <style name="Theme.Hummingbird.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0F172A</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Hummingbird</item>
    </style>
</resources>
